データパターン/データ構造に気をつける。
  ・データの一部がソートされているケース
  ・全くソートされていないケース
  ・値に重複があるケース
  ・値が文字列のケース
  etc

最悪時、平均時、最良時の分析をするとベスト

データパターンによって効率は変化するので、単一の最良アルゴリズムは存在しない。
対象ドメインのデータ仕様を把握することが大事。


定数項が結果に影響をおよぼすこともある。必ずしも無視できない。

全体的に数学要素が強く挫折しがち。
つまらなければこの章は飛ばす(飛ばした)
